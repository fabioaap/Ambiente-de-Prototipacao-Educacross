// Placeholder de testes para evitar falha do Vitest ao encontrar este arquivo legado.
// Os testes reais de screenshot foram movidos para `pixel.e2e.ts` executado via Playwright.
import { describe, it, expect } from 'vitest';

describe('pixel-playwright-placeholder', () => {
    it('deve passar (placeholder)', () => {
        expect(true).toBe(true);
    });
});
